// /comments/some-event-id (이렇게 라우팅해야 더알기 쉬움!)
function handler(req, res) {
  // [eventId.js]이므로!
  const eventId = req.query.eventId;
  if (req.method === 'POST') {
  }
  if (req.method === 'GET') {
  }
}
